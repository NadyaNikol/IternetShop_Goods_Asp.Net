@model IEnumerable<WebApplication2.Models.Good>

@{
    ViewData["Title"] = "Каталог товаров";
}


<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>View</title>
</head>
<body>
    <h5>Hello @ViewBag.user</h5>
<p>
    <a asp-action="Create">Create New</a>
</p>
@*<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FileName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Path)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>*@
<div>
    @foreach (var item in Model)
    {

        <div class="card" style="width: 18rem; float:left; margin:10px;">
            <img src="@Url.Content(item.Path)" class="card-img-top" alt="Нет изображения" style="height:200px; width:170px">
            <div class="card-body">
                <h5 class="card-title">@item.Name</h5>
                <p class="card-text">Цена: @item.Price грн.</p>
                @*<a href="#" class="btn btn-primary">Подробнее</a>*@
                @Html.ActionLink("Подробнее", "Details", new { id = item.Id }, new { @class = "btn btn-primary" })
                @Html.ActionLink("Редактировать", "Edit", new { id = item.Id }, new { @class = "btn btn-primary" })
                @Html.ActionLink("Удалить", "Delete", new { id = item.Id }, new { @class = "btn btn-primary" })
            </div>
        </div>

        @*<tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    <img src="@Url.Content(item.Path)" style="width:100px;height:100px" />
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                </td>
            </tr>*@
    }
    @*</tbody>
        </table>*@
</div>
</body>
</html>
